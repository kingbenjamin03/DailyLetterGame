# Sports stats: DB + fetch from Sports Reference (NFL, NBA, NHL).
# First fetch run can take ~1–2 min (one request per leaderboard, small delay).
# After that, data is in SQLite and queries are instant.
